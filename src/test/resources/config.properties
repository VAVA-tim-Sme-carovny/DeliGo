# Test configuration for SSL connection test
DB_PASSWORD=test
DB_URL=jdbc:postgresql://localhost:5432/test
DB_USER=test

# SSL Configuration
SSL_ENABLED=true
SSL_KEYSTORE_PATH=deligo.p12
SSL_KEYSTORE_PASSWORD=deliGoPassword